# Tasks: Animated Buttons Modal

- [x] Identify the modal component file.
    - [x] `search_file_content` for "EventContactModal".
- [x] Create/Update the animated button component.
    - [x] Implemented `AnimatedButton` in `src/components/ui/animated-button.jsx` with a "shimmer" effect and hover lift.
    - [x] Added `shimmer` animation to `tailwind.config.js`.
- [x] Integration.
    - [x] Import `AnimatedButton` in `EventContactModal.jsx`.
    - [x] Replaced "Cancel" and "Send Message" buttons.
    - [x] Styled with `bg-brand-primary` (via `default` variant) and hover effects.
- [x] Verification.
    - [x] Verified code structure and imports.